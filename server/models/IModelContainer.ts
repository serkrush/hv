import {asFunction, asValue} from 'awilix';
// import {dbSchema} from '../db';

export interface IModelContainer {
    // Users: typeof dbSchema.users;

    // Models: typeof dbSchema.models;
    // Machines: typeof dbSchema.machines;
    // MachinesGroups: typeof dbSchema.machinesGroups;
    // MachinesAccess: typeof dbSchema.machinesAccess;
    // Invitations: typeof dbSchema.invitations;
    // Categories: typeof dbSchema.categories;
    // Recipes: typeof dbSchema.recipes;
    // Cycles: typeof dbSchema.cycles;
    // RecipeFavorites: typeof dbSchema.recipeFavorites;
}

export default {
    // Users: asValue(dbSchema.users),

    // Models: asValue(dbSchema.models),
    // Machines: asValue(dbSchema.machines),
    // MachinesGroups: asValue(dbSchema.machinesGroups),
    // MachinesAccess: asValue(dbSchema.machinesAccess),
    // Invitations: asValue(dbSchema.invitations),
    // Categories: asValue(dbSchema.categories),
    // Recipes: asValue(dbSchema.recipes),
    // Cycles: asValue(dbSchema.cycles),
    // RecipeFavorites: asValue(dbSchema.recipeFavorites),
};
